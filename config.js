// EMS2 branch — backend deliberately disconnected until M3.
// The real Supabase configuration will replace this file in the M3 package.
// With API_URL empty, no module on this branch can reach the live backend.
const API_URL = '';
